apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-netflix-app
  labels:
    app: netflix-app
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      nodePort: 30008
  selector:
    app: netflix-app
